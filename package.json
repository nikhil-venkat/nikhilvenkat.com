{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "lerna run --parallel watch",
    "build": "lerna bootstrap && lerna run build --scope=shared && lerna run build --scope=app",
    "build:app": "lerna bootstrap && lerna run build --scope=shared && lerna run build --scope=app"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "author": "Nikhil",
  "license": "ISC"
}
